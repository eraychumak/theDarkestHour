import { KEYS, IMAGES, STYLES, TEXT, HTML, SPRITE_SHEETS, SOUND } from "../config.js";

export default class GameScene extends Phaser.Scene {
  constructor() {
    super({
      key: KEYS.SCENE.GAMEPLAY
    })
  }

  preload () {
  }

  create () {
  }
}